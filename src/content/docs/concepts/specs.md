---
title: Specs
description: A Spec is the underlying configuration Q uses to execute a Playbook. Most users never need to touch Specs. For developers, they are the extension point.
nextjs:
  metadata:
    title: Specs — the technical layer beneath Playbooks
---

<!-- SEO target: "ai agent spec", "agent configuration file". This page leans dev-ish but still opens with a plain-language "most users will never need this" note. Then developer detail: schema, tools, prompts, storage path. -->
